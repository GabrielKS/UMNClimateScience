import numpy as np  # Planning to use for data processing
import netCDF4 as nc  # Not used anymore
import Ngl  # Planning to use for graphics
import Nio  # Not used anymore
import xarray as xr  # Used to read and process NetCDF data
import dask  # xarray uses this behind the scenes

print(Nio.__version__)
print(Ngl.__version__)
